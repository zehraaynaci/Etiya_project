package com.example.demo_project;

import com.example.demo_project.Mapper.UserMapper;
import com.example.demo_project.Repository.UserRepository;
import com.example.demo_project.Services.UserService;
import jakarta.validation.constraints.Size;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;

import static org.mockito.Mockito.mock;

public class ServiceTest {
    /*
    private UserService service;
    private UserRepository userRepository;
    private UserMapper mapper;
     @BeforeEach
    public void setUp(){
         userRepository = mock(userRepository.class);
         mapper = mock(UserMapper.class);
         service = new UserService(userRepository, mapper);

     }
     @Test
    public void testFindByUserId_whenCustomerIdExists_shouldReturnUser(){
         User user = new User()
     }

     */
}
